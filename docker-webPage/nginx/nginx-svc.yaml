apiVersion: v1
kind: Service
metadata:
  name: web-nginx
  labels:
    run: web-service
#  namespace: tfg      
spec:
  type: NodePort
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: nginx
